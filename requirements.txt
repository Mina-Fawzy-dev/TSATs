# ============================================================
# REQUIREMENTS for the 3 Python tools:
#   1. system_info_tool.py
#   2. network_scanner.py
#   3. pc_health_monitor.py
# ============================================================
# To install everything at once, open a terminal and type:
#   pip install -r requirements.txt
#
# Or install them one by one:
#   pip install psutil
#   pip install scapy
#   pip install python-nmap
# ============================================================

psutil           # Used by all 3 scripts (CPU, RAM, disk, processes, temps)
scapy            # Used by network_scanner.py (fast ARP scanning)
python-nmap      # Used by network_scanner.py (deep port scanning via Nmap)
